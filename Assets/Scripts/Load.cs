using UnityEngine;
using System.Collections;
using System.IO;

public class Load : MonoBehaviour {
	
	public float ModeD;

	// Use this for initialization
//	void Start () {
//        StreamReader streamReader = new StreamReader("Data/Save/AppliedSettings.mps"); // Открываем файл
//		if(streamReader != null) {
//        while (!streamReader.EndOfStream) // Читаем строки пока они не закончатся
//        {
//			ModeD = System.Convert.ToSingle(streamReader.ReadLine());
//        }
//			{
//				transform.position = new Vector2(-460, -256);
//			else
//		}
//	}
}
